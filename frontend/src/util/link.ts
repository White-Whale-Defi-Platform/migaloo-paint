export const exploreTx = (hash: string): string => `https://ping.pub/whitewhale/tx/${hash}`
export const exploreAccount = (address: string): string => `https://ping.pub/whitewhale/account/${address}`
